#pragma once

// resolution of the output image
#define WIDTH		@DEFAULT_WIDTH@
#define HEIGHT		@DEFAULT_HEIGHT@

// number of samples per pixel
#define SAMPLES		16

#define EPSILON		0.001f

// depth limit of tracer recursion
#define DEPTH_LIMIT	16
#define RAY_ERROR	0.002f

/* Save some code bytes and let the OS take care of some resources
   which we don't free explicitly */
#define LEAK_RESOURCES_ATEXIT
